// Question print the a to z letters by use of js but not use the variable whose data type is character or object or array or string just use the integers data type variable

console.log(String.fromCharCode);
for (let i = 0; i < String.fromCharCode.length; i++) {
  const element = array[i];
  console.log(element);
}
